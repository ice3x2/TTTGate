# Issue #47: full HTTP/TLS tunnel evidence

Status: full approved test-only helper/E2E stage completed; seven selected suites61PASS42.706s and build0, frozen for two independent reviews. Original default-CN positive-assumption failure remains preserved. Control branch fix/epic61-http-tls-e2e, base514d882. Author fix_lint_diagnostics. No production change or commit authorized.

## Root-approved full finite E2E implementation assignment

Status: both SAN baseline reviews PASS; root now authorizes test-only full stage on this branch. Earlier probe-only restrictions describe that checkpoint. Existing probe remains unchanged. Exact writes: test/helpers/tunnelHarness.ts (endpointFactory/trustedControlTls), test/e2e/tunnel/http-tls-tunnel.test.ts, optional single test/fixtures/http-tls-endpoint.ts, this ledger. network.ts/runtime/production remain read-only. No commit by this author.

- [x] Freeze helper contract and actual-case plan. Status: optional endpointFactory returns owned port/async close; immediately register cleanup and reject custom-owner echo calls before I/O. trustedControlTls defaults false; true uses owned SAN certificate-input fixture during Store.load with finally restoration, actual CA/serverName localhost and all client/server TLS. Reject conflicting explicit TLS/CA/name/insecure overrides before allocation. Preserve existing identities, aliases and unrelated overrides.
- [x] Observe RED before helper changes. Status:a3df15/c3778b session78965 naturalexit1,10failed26.943s. Five conflicting TLS inputs incorrectly returned allocated harnesses; custom endpoint factory was never called; its second-factory failure never occurred. Three full-path cases failed the explicit endpoint-capability precondition before HTTP traffic. These are helper API/behavior REDs, not production transport failures or claimed TLS/HTTP RED.
- [x] Minimum helper extension. Status:endpointFactory ownership/custom echo rejection and trustedControlTls SAN certificate setup only; no production/network/runtime edits.
- [x] Selected regressions/build. Status:595cfb/92e042 session52366 naturalexit0,seven suites61PASS42.706s; forced build777797 exit0. Exact commands below.
- [ ] Two independent reviews. Status: parent to delegate; no public/internal/admin TLS, #29 EOF, #53 spill or #28/#40 policy claim.

## Full-stage execution and evidence boundaries

RED command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/e2e/tunnel/http-tls-tunnel.test.ts`. Only afterward the existing harness gained optional per-owner endpointFactory and a small generic owned endpoint shape with an optional default EchoServer reference. Custom endpoints reject sendAndReceive before the once helper runs. Immediate endpoint ownership enables existing creation/start failure cleanup; a real first custom listener followed by a deliberately rejected second factory confirms closure/root removal and the original Error.

trustedControlTls defaultsfalse and owns positive server/client TLS, CA, serverName and verification fields. Explicit overrides of those owned fields reject before createTestRoot. Existing unrelated client/server options and #54 identity/ACL/multi-ID rules remain. In true mode generateSelfSignedCert('localhost') supplies an actual SAN certificate through the owned Store.makeTempCert only during original Store.load, finally restored. getTempCert then supplies the exact CA; all clients use TLS/serverName localhost/allowInsecureTls=false. This is disclosed certificate-input substitution; every connection and native verification still runs the real implementation. Existing #48 network helper remains unchanged.

Initial candidate730d1c/1078f7 session55651 naturalexit1,9passed/1failed11.720s. The encrypted case had successful HTTP/body/TLS results but read current localPort from a data socket after its finite FIN cleanup, producing a failed server-event match. Test-only correction records localPort at actual secureConnect. Selected5e28f6 naturalexit0,1passed/9filtered3.255s: printed observations show one closed data socket with no current localPort, its recorded secure-time port still matches actual server secureConnection. This is an observation-lifetime fixture correction, not a production repair or unexplained retry. No helper change followed it. Restart test then additionally requires actual old HTTP socket close and excludes its request-deadline error as a valid restart terminal.

Full-stage ten cases include five TLS conflict cases, custom-owner echo misuse, actual partial endpoint-factory cleanup, plaintext/encrypted mixed-owner HTTP/TCP, and encrypted actual server restart. HTTP endpoint records exact POST/path/rewritten destination Host/unchanged custom header/request body. It holds the first CL response until a different authenticated TCP owner completes exact binary echo; then both first CL and second chunked HTTP bodies rewrite to their distinct public Host contexts. Node HTTP end/complete/status/body and reuse of the same external agent socket are asserted, with exact endpoint records excluding cross-routing. The HTTP internal/public legs are plaintext here; the encrypted variant applies TLS specifically to tunnel control/data.

TLS observation delegates actual SocketHandler.connect and TCPServer.create, capturing real client native secureConnect/authorized and corresponding server secureConnection by the secure-time socket port. Both control owners and both data-channel roles are verified. After the healthy full harness is online, a separate owned native TLS socket uses the same CA but wrong hostname; it must report ERR_TLS_CERT_ALTNAME_INVALID, secure0/authorizedfalse/applicationbytes0. Subsequent healthy TCP owner echo succeeds. This negative control does not claim a negative data-channel handshake because it never authenticates control. The native probe retains the independently verified default-CN rejection and SAN-positive baselines separately.

Restart coverage sends one /inflight HTTP request whose owned endpoint intentionally withholds a response, restarts the actual shared server, requires that old actual socket closes without the helper deadline manufacturing success, then uses the existing client scheduler readiness and a fresh finite HTTP request plus TCP echo. Endpoint paths must be exactly /inflight then /fresh: no automatic resend. The interruption is expected; no in-flight payload survival or unknown-length HTTP EOF completion is claimed.

Exact final regression command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/e2e/tunnel/http-tls-tunnel.test.ts test/component/tls-temp-cert-contract.test.ts test/component/multi-client-harness.test.ts test/component/roundtrip-once.test.ts test/e2e/tunnel/baseline-tunnel.test.ts test/component/http-body-encoding.test.ts test/component/client/TlsVerification.test.ts`. Seven suites61tests PASS42.706s (595cfb/92e042) is one actual run, not a sum with selected5e28f6 or prior probes. `npm run build -- --force`777797 naturalexit0, existing npm always-auth warnings only. No live handle remains; no process termination or denied artifact operation.

Final five-file candidate: existing native probe, new HTTP endpoint fixture, new HTTP/TLS E2E test, existing tunnelHarness, ledger47. During full-stage implementation only the latter four were changed; the approved native probe stayed unchanged. Protocol producers, codecs, SocketHandler/TCPServer/TTTServer/CertificationStore source and network.ts remain untouched. Proposed title: `test: 실제 터널의 HTTP와 TLS 왕복 검증`.

- [x] Read existing certificate/runtime helpers and assignment. Status: CertificationStore.load/getTempCert and CACertGenerator reused; actual default CN is Testing CA - DO NOT TRUST, not localhost.
- [x] Execute one owned native TLS baseline probe. Status: d7f213 natural exit1, one failed test3.176s; observation4.652s, generation2.637s. Root budgets were test30000ms/observer60000ms, neither reached. No retry/verification disable.
- [ ] Root assess result and independently review before further fixture work. Status: subsequent harness/API changes require their own actual RED and assignment. This probe is baseline coverage, not production defect RED or completed #47.

The probe will load the existing generated temp certificate inside a fresh owned root, trust that exact certificate, use its actual CN as serverName, and require native secureConnect/authorized plus finite exact application echo. Track all owned accepted/client sockets, close the listener and verified root, preserve nonsecret facts/raw child output in a separate new evidence root. No arbitrary endpoint, process-name termination, denied path or benchmark. A watchdog expiry is failure; do not terminate a PID without current command-line and ownership verification.

Proposed exact child command: `node node_modules/jest/bin/jest.js --runInBand --runTestsByPath test/component/tls-temp-cert-contract.test.ts`. No forceExit, timing benchmark or whole-suite execution.

Actual command matched the proposal. Parent used native spawn with hidden window, captured child close/status/signal and a60000ms observational timer (no automatic kill). Evidence root `C:/Users/beom/AppData/Local/Temp/tls47-probe-evidence-JwqlkZ` retains result.json/stdout.txt/stderr.txt; owned child PID46408 closed naturally status1/signal null, expired=false. Native verification failed with ERR_TLS_CERT_ALTNAME_INVALID despite serverName matching the displayed generated CN `Testing CA - DO NOT TRUST`. The owned server reported socket hang up. This demonstrates that the proposed default-certificate name mapping cannot currently support the positive probe; it is not a new production defect RED. No successful TLS/authentication/echo is claimed. Facts record canonical root cleanup completed, elapsed2674ms. No processes terminated, no production/harness file edited, no metadata/certificate secrets published. Root must select a bounded owned valid-name test-certificate setup and obtain independent review before further execution; never set rejectUnauthorized=false or override identity verification to pass.

## Approved SAN fixture amendment

- [x] Root approve wave0-reviewed certificate-input boundary. Status: keep original d7f213 failure/raw evidence unchanged; convert default-CN case into explicit negative verification control and add a separate localhost SAN positive case. Reuse generateSelfSignedCert('localhost'); override only the owned CertificationStore instance's public makeTempCert during load, returning the real generated CertInfo, then restore in finally. Store publication, native TLS and hostname verification remain actual implementations.
- [x] Run amended two-case baseline. Status:a18be1 natural exit0,one suite/two testsPASS5.844s; outer7.278s, observer expired=false. Per-case30000ms/observer60000ms unchanged; no production RED, hidden retry or verification bypass.
- [ ] Independent two-reviewer acceptance before any harness implementation. Status: root coordinates after actual result; scope remains this test and ledger only.

Amended baseline command remained `node node_modules/jest/bin/jest.js --runInBand --runTestsByPath test/component/tls-temp-cert-contract.test.ts`. Evidence `C:/Users/beom/AppData/Local/Temp/tls47-san-evidence-4cAc1r` preserves raw stdout/stderr and result.json for owned child62168, status0/signal null/expired=false. Default-CN negative: generation2691ms, case facts2731ms, ERR_TLS_CERT_ALTNAME_INVALID, client/server secure events0, authorized=false, client/server applicationbytes0. Separate SAN positive: generation2650ms including actual Store.load work, case facts2664ms, actualCNlocalhost and DNSlocalhost/IP127.0.0.1 SAN checked, client/server secure events1, authorized=true, exact26byte request/response. Both canonical owned roots were verified and cleaned; evidence root remains preserved. No process was terminated. Initial d7f213 failed hypothesis is unchanged, not retroactively GREEN; these are explicitly different negative/positive baseline contracts. Full TTTServer/TTTClient HTTP/data forwarding is not yet exercised by this native probe and cannot be claimed complete. No extra build, whole-suite or production change was performed.

## Final independent candidate reviews

- [x] Full candidate and exact-title reviews. Status: review_wave0 and review_cert_conflict both PASS with no remaining Critical/High/Medium/Low findings. Earlier unchecked review/assignment entries describe their historical checkpoints. Final seven-suite61PASS42.706seconds/build0 and original failed observations remain separately recorded above.
- [ ] Root source commit, serial integration, regression, push and issue notification. Status: pending. Main source integration waits until the independently owned #49 five-suite observation finishes, so its source checkpoint does not change during measurement.

Exact approved title: `test: 실제 터널의 HTTP와 TLS 왕복 검증`. This ledger-only recording changes no source, fixtures, tests or preserved evidence.