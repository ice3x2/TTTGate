# Issue #60 — Owned test artifacts

Status: implementation, canonical-alias correction, focused contracts and both final independent rereviews PASS; source frozen for root commit/integration.
Owner fix_supply15; listeners worktree branch `fix/epic61-test-artifacts`, base `f3c2a10`.
Approved research: main `epic61-issue-60-research.md`; consult its concrete R/E mapping.
Writable: req04 constant-time test artifact case only, req09 pool-swap report lifecycle only, dedicated artifact sandbox/contract fixture and this ledger. No timing code/threshold/execution, #59 helper, scanner, production or historical artifact cleanup.

- [x] Read exact independent sandbox research and current callers. Status: complete.
- [x] Create new R/E and regular-file manifest with hashes. Status: explicit paths and manifests below; complete src and selected import closure, no links/dependency copy.
- [x] Observe actual selected-case artifact/cleanup RED. Status: four contracts failed because actual reports remained after selected real cases completed/failed; no setup/resolver error.
- [x] Minimal path/lifecycle change and successful/failing cleanup checks. Status: four focused contracts passed, child failure1 preserved as expected failure evidence.
- [ ] Independent rereviews after M1. Status: original review found normal parent-alias mismatch; separate fixer observed actual child RED and corrected canonical capture. Final checks below; root coordinates rereviews.

Existing main/release test/.tmp-req04-lint is outside all copy/run/cleanup targets and remains untouched. R/workspace is child cwd; R owns TMP/TEMP and Jest cache; dependencies/transformer are explicitly read-only original node_modules. E separately preserves manifest/hash/log/report evidence. No R deletion is planned until evidence is captured and exact realpath/lstat ownership checks pass.

## Actual RED

Prepare command: `node test/fixtures/artifact-sandbox.cjs prepare`.
RED R: `C:/Users/beom/AppData/Local/Temp/artifact-60-R-VXlI6I`; E: `C:/Users/beom/AppData/Local/Temp/artifact-60-E-gK917C`. Manifest lists regular source/destination files and SHA256, original Jest/transformer dependency paths and sandbox config; no dependency copies or links. Main assignment record was confirmed before selected execution. Complete src is copied; original scanner input remains `src` under copied workspace.

Contract command: `node test/fixtures/artifact-sandbox.cjs <mode> <E>`. Modes lint/pool select anchored full names recorded in the fixture: only req04 lint(a), only req09 actual harness(3b). Neither semantic/timing case nor restart stress ran. Actual content is captured by a delegating fs observer after real write/append; the scanner child report is read before cleanup. The failure mode throws the disclosed `owned-artifact-assertion` only after the actual JSON/CSV metric is written, and tests original failure propagation plus teardown. No metrics are mocked.

Before source changes: lint851363/pool91f9d4 had selected child exit0 but outer contract exit1 because reports remained. lint-failure2ee6d9/pool-failure9a0c7d had selected child exit1 with the injected marker and outer contract exit1 because the reports remained. JSON scan/filtered content and real `3b,range_guard,pass` CSV are captured in E; each mode has run/artifacts/leftovers JSON. All four failures are retained; no old artifact path was touched.

## Minimum repair and GREEN

The two report producers now allocate fresh report roots only when selected/reporting. req04 shares its root between scanner/filtered JSON and cleans in finally; req09 lazily allocates on metric append and cleans after harness disposal. A fourteen-line dedicated test helper `test/helpers/artifactRoot.ts` reuses mkdtemp ownership for both and checks real directory/lstat/realpath before removing its exact recorded root; it does not search or delete siblings. Cleanup errors surface. This avoids duplicating ownership validation in both test files and is solely a fixture helper, not a production abstraction.

GREEN R: `C:/Users/beom/AppData/Local/Temp/artifact-60-R-eCKb4D`; E: `C:/Users/beom/AppData/Local/Temp/artifact-60-E-gPkJ14`, separately prepared from repaired source with its own manifest/hash record. Same four commands: lint ff1de6/pool4f76cb outer exit0 with child0; lint-failure3604db/pool-failure0e6a45 outer exit0 with expected child1 and injected-error evidence. Actual generated bytes were captured before deletion; no generated reports remain in the fresh workspace. Failure-child1 is not relabeled a passing Jest case; only its cleanup contract passes.

R and E roots are preserved for review, not bulk-deleted. The report-root cleanup assertions are scoped to the actual newly captured report directories. No compiler/runtime/benchmark changes or #59 file writes occurred. Final independent review must compare the unchanged timing suffix and regular-file manifests. Proposed title: `test: 검사 산출물을 소유 임시 경로에서 정리`.

Final contract strengthening additionally requires each captured report's parent root to be absent, not merely its files. Success checks6d3df3/c20622 passed with child0; failure checks188d46/199d7f passed with child1 and the original injected marker. Final failure receipts are uniquely preserved under E/`lint-failure-run-TlPB4F` and E/`pool-failure-run-pOuYuO`. The driver now allocates a unique evidence subdirectory per execution to avoid overwriting future runs. Earlier successful mode files hold their last strengthening run; their prior tool receipts remain above. Original RED modes were not rerun or overwritten. Six-file scope is frozen for independent review; no live handles remain.

## Independent fixer: normal temporary-parent aliases

Root assigned review_cert_conflict as a different fixer for review_wave0's M1;
original author remains fix_supply15. Only artifactRoot helper, dedicated contract
test and this ledger may change. The new test creates an alias whose target is
strictly inside a newly owned outer root; it changes child-temp environment only
during actual mkdtemp, then restores it. It writes a real artifact and requires
normal helper cleanup/canonical root identity while preserving a sibling file.
Before outer cleanup it removes the alias nonrecursively and verifies each real
owned path. No shared dependency junction or historical directory is involved.
The separate newly owned evidence root is preserved. RED is pending before helper
edits; the original two test cases' semantics/benchmark remain frozen.

Initial alias-contract attempt de6f15 exited1 (0.385s) before any helper edit.
It is a fixture failure, not cleanup RED: changing Jest's process.env did not
change native os.tmpdir, so creation occurred at a separate newly allocated temp
root rather than below the alias. The ownership assertion stopped recursive
cleanup of that out-of-fixture path; no historical/denied path was deleted.
Receipt E=artifact-alias-60-evidence-hVzIky is preserved. The fixture now launches
an actual child with TMP/TEMP/TMPDIR set before Node initialization, records its
canonical created path/stage, and tests cleanup only after verified owned creation.

Corrected alias RED 457453 exited1 naturally, one failed test in0.903s. The actual
child recorded createdWithinTarget:true and reached cleanup, which rejected its
lexical alias/report path as ownership-changed despite the target being owned.
E=artifact-alias-60-evidence-1TQF4A preserves that real receipt. Only after this RED,
the helper records realpathSync(mkdtempSync(...)) and removes the now-unused resolve
import. Cleanup ownership validation itself is unchanged; no comparison is relaxed.

Alias GREEN: same dedicated Jest command, 720e69 natural exit0, one test PASS in
0.719s; E=artifact-alias-60-evidence-hsvasJ. The child records the real created path
below the owned alias target and completes cleanup. The sibling file survives;
parent removes the test alias nonrecursively before removing its validated fresh
outer root. No dependency link is created. Both previous failure evidence roots
remain preserved; de6f15 is still excluded as fixture failure rather than RED.

Fresh ordinary-contract preparation after the correction (33f863) produced
R=`C:/Users/beom/AppData/Local/Temp/artifact-60-R-ZgqS0S` and
E=`C:/Users/beom/AppData/Local/Temp/artifact-60-E-7FlhdT`. Its manifest copied the
corrected helper and the unchanged selected scanner/harness cases using the
approved read-only dependency mapping. Four serial executions all returned outer
exit0: lint1278bd child0 (lint-run-HtrRDp), pool2ab7fc child0 (pool-run-Liv0VG),
lint-failure4b3b75 child1 (lint-failure-run-2RhieS), pool-failure1d2f6e child1
(pool-failure-run-QXoe2N). The two child1 results retain the injected post-write
assertion failure; only their actual-report cleanup contracts pass. Real report
bytes and root/file absence checks remain enforced. R/E are preserved, not swept.

Forced `npm run build -- --force` (faf716) exited0. Fixer changes are limited to
canonical capture/unused import in artifactRoot.ts, new artifact-root.test.ts and
artifact-root-alias.cjs contracts, and this ledger. No production, scanner, #59,
original two tests' benchmark/semantic code or historical denied paths changed.
The resulting full #60 write set has eight files; original author fix_supply15
and separate fixer review_cert_conflict remain distinct. Source/test/ledger are
frozen for two other reviewers. Existing proposed title remains
`test: 검사 산출물을 소유 임시 경로에서 정리`; nothing is staged or committed.

## Final independent rereview receipts

Root confirmed review_wave0 and fix_lint_diagnostics final M1 rereviews both PASS with zero Critical/High/Medium/Low findings and existing exact-title PASS. Neither reviewer performed additional execution for the final rereview. review_wave0 read the actual owned alias RED/GREEN receipts and fresh four-mode R/E records; canonical creation fixes legitimate parent aliases without relaxing cleanup ownership. Successful child0 and expected injected-failure child1 remain distinct from their outer cleanup-contract results. Prior setup failure remains excluded from behavior RED. No benchmark, denied path or producer/runtime change was inferred.
This entry was written by review_wave0 only; source/test files stay frozen. Root coordinates independent confirmation and subsequent commit/integration.
