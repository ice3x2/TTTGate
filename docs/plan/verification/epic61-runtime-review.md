# Independent review: Node 24 and comparison verification

Status: final source, method, forced-compilation, regression-evidence and refreshed-binary reviews PASS; combined commit `b093dce` pushed and #63/#64/#66 closed/notified. Authored documentation awaits separate rereview. Reviewer: `review_wave0`. Date: 2026-09-08 KST.

- [x] Review #63 runtime configuration and dependency scope. Status: final PASS; all five Node 24 targets, runtime/admin/distribution engines, workflows and Node declarations are aligned. Initial artifact checks independently verified native Windows product startup, a packaged Node 24.18.1 probe and five PE/ELF architectures. Those initial artifacts predated #66/compiler fixes; the final regenerated artifacts have now been checked independently as recorded below. Earlier incremental build success is not evidence of a forced clean compilation.
- [x] Review #64 implementation and independent fixes. Status: PASS; initial HIGH mean-to-max denominator change was corrected after a boundary RED test, restoring the original 7% meaning. MEDIUM missing `-r`/global-coverage rejection was fixed by independent fixer `fix_ci14`. Independent dedicated run passed 19 contract/guard tests in 1.331 seconds without running timing measurements.
- [x] Review the preregistered diagnostic before measurement. Status: PASS; five groups, 50,000 total warmup calls per group, 120 AB/BA pairs with 10,000 calls per label, rotating group order, twelve fixed ten-pair blocks and descriptive t11 intervals. Invalid controls are INCONCLUSIVE; valid controls with pooled target bias at/above 7% are FAIL; otherwise imprecise intervals are INCONCLUSIVE. Raw data are retained without sampling retries or outlier removal. Interval interpretation explicitly acknowledges unknown serial dependence.
- [x] Review #66 parity-only runtime change. Status: PASS; odd-length hex strings invalidate format without skipping the existing padded native comparison/dummy path. Other encodings and Buffer inputs retain behavior. Independently ran two suites/24 comparison tests, exit 0, 3.698 seconds. An earlier attempt during dependency replacement did not start Jest; a separate PowerShell startup failure also did not execute tests. Neither is product regression evidence.
- [x] Independently review final forced compilation, ordinary full regression and refreshed five-platform binaries. Status: PASS. Worker forced build handle `45821` exited 0, followed by full regression `16537` exit 0 (75 suites/372 tests passed, four online tests skipped, 376 total, 228.365 seconds), then packaging `33884` exit 0. Reviewer independently reran forced compilation without diagnostics, nine runtime-contract tests passed, and checked final artifact headers/hashes plus native executions. No whole-suite rerun was needed.
- [ ] Independently rereview this authored documentation. Status: pending a different reviewer; this review author does not self-approve evidence text.

## Timing results and limits

The original ordinary Jest failure was 15.779504434% against 7%; a later unchanged focused observation was 1.524376419%. These outcomes were investigated without attributing a cause to concurrent packaging or claiming a runtime vulnerability.

The first explicit experiment report `req-04-2026-09-07T15-38-30-092Z-20236-54902eaf-fcfb-4174-83cf-c98fa5449144.json` remains FAIL. Arithmetic-only reanalysis with the original mean denominator yields target 10.395414392%, noise 5.361046057%, positive control 178.371677302%. Its raw samples remain unchanged; reanalysis is not a new measurement.

The preregistered five-group report `req-04-2026-09-07T15-56-17-366Z-132320-8ae9248a-dbfb-4997-ae35-a44bb80a35a7.json` is INCONCLUSIVE: hex noise 9.866448268% exceeds 7%. Target pooled values below 7% do not override a failed control.

The one approved affinity comparison report `req-04-2026-09-07T16-03-07-856Z-114204-0451ede2-8abd-4ed1-8870-1fbaf897ce65.json` is INCONCLUSIVE: controls pass, but the decoded-Buffer target interval is [-3.357729482%, 14.167282011%]. Its pooled target bias is 3.789382287%; the hex target pooled bias is 5.713328750% with interval [-6.551348299%, 2.826477717%]. This is not an overall measurement pass.

Launcher evidence `affinity-launch-a86b58f9-7b54-4d35-8cf0-1190737d8e33.json` records allowed mask FFFFFFFF, selected/observed mask 1, actual PID 114204 and Normal priority. Native exit code was unavailable; the outer launcher exit 0 cannot establish success. The same script/helper hashes were recorded. Restricting affinity does not make the shared host exclusive or prove the cause of earlier variability. All reports reside in the runtime worktree's `results/timing/` and must be retained in final evidence.

Root accepted the verification-method repair disposition: #64's written acceptance does not demand a conclusive physical timing proof. Final ordinary regression and result-evidence review passed; it can close as a corrected, explicit, fail-closed verification method while preserving these empirical limitations. Do not describe it as a passing benchmark. No more timing measurements or condition searches are planned.

## Proposed combined commit

Final compiler scope: root and admin lock TypeScript 5.9.3, compatible with the installed Node 24 declarations; the unused deprecated `@types/rimraf` stub was removed after a forced-build failure identified its implicit declaration chain. The runtime rimraf dependency remains. `skipLibCheck` remains false; no source imports of rimraf were found. Root/admin engines, lock roots and all workflow Node setup contracts pass.

Reviewer independently checked the final generated five files against the issue #63 ledger: all lengths, PE/ELF machine codes and SHA-256 hashes match. Final Windows x64 product prints TTTGate 1.0.11b usage and exits 0; the rebuilt native probe prints Node 24.18.1, `packaged: true`, `sourceReadable: true`, and expected SHA-256 `d049666b78fca8bba8e24ba2b8d8691f1ddfdb2c5421e0941675ccda2b914d53`, exiting 0. Cross-platform execution and release archive/upload correctness remain separate, explicit limits (#44/#65); file-header checks are not represented as running those platforms. Native binary final hash is `8907305b5409b1c0e9f421d58e949735764534b955a4c13ea48760bb85b1e442`.

`fix: Node 24 지원과 안전한 비교 검증 보강` describes the combined #63/#64/#66 changes and contains no signature or progress marker. Message content/compliance review: PASS. The proposed body accurately covers minimum Node 24, workflows/types/packaging, odd-hex rejection with native comparison preserved, ordinary security checks versus explicit timing experiments, 372 passing tests and five binaries, with timing INCONCLUSIVE and no physical constant-time proof. Combined changes integrated and pushed as `b093dce`; generated binaries remain verification artifacts, while intentional raw evidence is preserved byte-for-byte through exact-path Git attributes.
