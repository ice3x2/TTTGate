# Issue #58: original SRS trace structure and semantic batches

Status: CLOSED2026-09-09T02:32:44Z, comment5594889018, Telegram4002(68/64). Final data commitd4efb8948c567c36b1a5e222580e5e3a9ec85472 pushed. Complete trace inventory:366 original rows/142candidate links/108linked rows/258gap-only rows/97unique tests. Runtimeverified0/evidence0: closure covers mapping and explicit gap inventory, not proof that every original behavior is implemented or executed. Prior draft/publication pending checkpoints below are historical.

- [x] Preserve immutable original source inventory. Status: all366 scoped IDs remain required; approved hashes651898fa44ac7c3e037c4cc3706837062105871c7b8f41f92c66f728842b149d and364071a1f67e07501e7277e15ebdac057eb17ae8e6c2e076a3a550d916625657. No original bytes/comments changed.
- [x] Prepare structural test before implementation. Status: test/unit/tools/srs-trace-validator.test.ts defines owned-source-copy fixtures, default read-only validation, explicit deterministic view rendering, complete IDs/hash/heading/type, exact case/parameter links, reverse bridge and false-verified rejection. Historical preparation: tests were held while the first #49 ordinary138-suite run was active; later executions are recorded below.
- [x] Observe structural contracts and freeze diagnostics. Status: original missing-script9FAIL remains capability absence; later actual alias/dynamic-title2FAIL exercises validator behavior. No production/runtime RED claimed.
- [x] Implement validator and initial unverified366 canonical rows after RED. Status: historical bootstrap created all366 rows before semantic mapping; first100 are now applied as recorded above.
- [x] Draft semantic batch01. Status: docs/plan/verification/epic61-issue-58-draft-batch-01.json contains only original sidecar positions1..25, exact headings/types and manually selected implementation/test candidates/gaps. The initial draft was unreviewed; subsequent independent corrections/reviews and first100 publication are recorded below. No runtime verified promotion or execution receipt was invented.
- [x] Independent structural and first100 candidate mapping reviews. Status: structural final18PASS and separate fixer cross-reviews completed; batches01..04 clause/candidate/gap placement independently reviewed. First100 canonical publication preserves runtime unverified status; this does not close #58.
- [x] Independent candidate publication review and complete mapping draft. Status: all366 rows are published with142 candidate links after independent semantic, normalization and render reviews. Runtime evidence and gap disposition remain separate incomplete gates.

## Historical initial preparation and structural checkpoints

Proposed concrete JSON envelopes for the approved row schema: matrix `{schemaVersion:1, sourceManifest:{path,sidecarPath,documentVersion,markdownSha256,sidecarSha256}, rows:[...]}`; generated bridge `{schemaVersion:1, sourceHash, entries:[{testPath,exactTitle,parameters,links:[{sourceHash,originalId,state,assertedClauseRefs}]}]}`. Markdown and bridge are deterministic views of the matrix, not alternate editable authorities. Source paths are relative to --source-root; default output view is the matrix's neighboring original-srs-matrix.md and bridge is source-root/test/srs-trace-index.json. --write-view explicitly renders both; default validates agreement without writing. These are test-contract decisions for independent review, not executed artifacts.

Future exact selected command: `node node_modules/jest/bin/jest.js --runInBand --runTestsByPath test/unit/tools/srs-trace-validator.test.ts`. It must not execute until the current ordinary run finishes. Per-call CLI contract: `node scripts/validate-srs-trace.mjs --matrix <owned-matrix> --source-root <owned-root>`; valid structure0, invalid structure1 with useful bounded path/ID diagnostics, --write-view optional explicit rendering. No app imports/network/coverage. Test corruption fixtures never modify original SRS or existing tests. A missing-script failure cannot accidentally satisfy negative tests because diagnostics must identify the corrupt original ID/path rather than generic module errors.

Semantic draft is based on current code/test reads only. Historical SRS source line ranges are retained as source metadata and are not reused as current proof. Candidate titles include describe hierarchy/parameter identities when resolved; absent evidence stays an explicit gap. Existing REQ comments use document-qualified aliases with source hashes when actually located; uncertain aliases stay absent with a gap instead of guessed joins. Actor/context items are not automatically verified or exempted if they include behavior. Implementation located and tests candidate do not imply acceptance. Actual verification metadata remains empty until genuine matching execution and independent semantic review.

No test/build/validator/script execution, implementation edit, threshold change, process termination or commit performed. #53/#73 production work is separate. #49/#52/#53 and policy/held dependencies remain independent gates; matrix366 presence alone never completes them or all requirements.

## Root-authorized structural execution

Status: root released the short validator-only RED/GREEN stage after full49 terminal. Freeze per-test Jest budget30000ms (initial budget, not a measured duration). Exact command: `node node_modules/jest/bin/jest.js --runInBand --testTimeout=30000 --runTestsByPath test/unit/tools/srs-trace-validator.test.ts`. No E2E/coverage/full npm/build or new dependency. Canonical bootstrap will contain all366 original rows unverified; separate first25 draft cannot be merged or promoted without semantic review. Existing source/tests/SRS bytes unchanged; exact approved new validator/matrix/view/bridge paths only.

- [x] Observe first contract execution before validator implementation. Status:e4ad22 naturally exited1,9FAIL1.089s; every failure was missing validator capability, not observed malformed-structure rejection. This distinction is retained.
- [x] Initial validator and complete366 bootstrap. Status: minimal script uses existing TypeScript declaration parser without executing imports/tests; all original IDs/types/headings/hashes checked. Canonical JSON contains366 unverified rows and no candidate draft merge. Initial contract2719e1/98c948 naturally exited0,9PASS17.528s; actual corruption checks now run and reject wrong IDs/hash/headings/types/false verification and bridge mismatch.
- [x] Complete exact alias/dynamic-title controls and rendering. Status: after explicit root window, selected c94626 naturally exited1,2FAIL/9filtered0.977s. A correctly document-qualified many-to-many alias was wrongly rejected; a test inside dynamic describe.each incorrectly resolved without its parent. Minimal correction permits explicit qualified many-to-many links and carries unresolved parent context through parsed declarations. Dynamic candidates require titleResolution:unresolved and can never become verified from that unresolved declaration.
- [x] Final structural contract GREEN. Status:fe29c0/97861f session62853 naturally exited0,one suite11PASS18.782s under the fixed30000ms per-test limit. This is separate from initial9PASS17.528s; no synthetic20-test run.
- [ ] Independent final structure/artifact review. Status: no automatic semantic verification or commit; root assigns reviewers.

First GREEN was already launched when root's #73 overlap notice arrived; no further run was started and the existing lightweight validator-only handle was observed to its natural end. Subsequent execution waits for root serial signal. This was not E2E, build, coverage or full npm. The source manifest and canonical artifact remain separate from the first25 semantic draft.

Final renderer e154f5: `node scripts/validate-srs-trace.mjs --matrix docs/plan/traceability/original-srs-matrix.json --source-root . --write-view`, naturalexit0. Default read-only check9ff9b3 used the same arguments without --write-view and exited0. Both report366 original rows while explicitly disclaiming semantic verification/runtime receipt authenticity. Generated bridge contains no invented links because all bootstrap verification arrays are empty. Original imported files and existing test/source bodies are unchanged. The separate first25 draft remains candidate/unreviewed and awaits semantic review; the root's prior draft corrections remain intact.

Exact final contract command: `node node_modules/jest/bin/jest.js --runInBand --testTimeout=30000 --runTestsByPath test/unit/tools/srs-trace-validator.test.ts`. Additional RED appended `--testNamePattern "document-qualified legacy requirement|parameterized describe context"`. No heavy/E2E/coverage/build/package execution or production change. No live handle remains; root may release the serial window to #73. Final new artifacts are the approved validator, contract test, canonical JSON/view, test bridge, this ledger and the separate already-prepared25-ID draft. Structure PASS alone does not promote any original ID or close #58.

## Independent validator M2 correction

- [x] Actual negative-fixture RED before changing validator. Status: review_cert_conflict (different fixer) added six contracts. 12bdc5 naturally exited1:6failed/11filtered3.586seconds against original validatorSHA191357FDAA96FFA8A648E5344F664C0BFF2C7AD579969600F964672DA56B926F. Explicit test.skip, it.todo and a nested describe.skip parent all remained valid candidates but incorrectly passed as verified; string/object/null acceptance.clauses likewise incorrectly returned0. These were actual CLI schema-validation failures, not module/setup absence.
- [x] Minimum validator change. Status: declaredTests carries disabled title metadata through parent describe contexts; verified links to disabled declarations reject while candidate associations remain permitted. acceptance.clauses must explicitly be an array; the old array() fallback no longer hides malformed input. No canonical row/view/SRS source change.
- [x] Full structural contract GREEN/default read-only validation. Status:a03e55/49636/0cabae naturally exited0:17testsPASS22.357seconds, followed by default validator0 on actual366-row matrix. It explicitly states semantic verification/runtime authenticity are not established. Original11PASS18.782 and earlier RED history remain separate.
- [x] Independent rereviews/root commit. Status: subsequently completed for candidate mapping/publication; historical checkpoint: only scripts/validate-srs-trace.mjs, test/unit/tools/srs-trace-validator.test.ts and this ledger changed by this fixer. Frozen for two other reviewers; no commit. #73 production remains frozen in its separate worktree.

RED command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/tools/srs-trace-validator.test.ts --testNamePattern='disabled|non-array'`.
GREEN: same complete suite without filter, then `node scripts/validate-srs-trace.mjs --matrix docs/plan/traceability/original-srs-matrix.json --source-root .` (no --write-view).

Final validator SHA88D5C933A705F1D2984E29559A44EFE3FD95280A2438456BAF69C2DBE771652F. Source SRS hashes remain651898...49d and364071...657. Disabled-fixture fake receipt/reviewer metadata exists only in owned temporary structural tests, explicitly marked fixture-only; it is not real runtime evidence or a promotion of any canonical requirement. Each case also enables the exact same declaration and confirms structurally complete verified metadata can pass; rejection is specifically due to disabled status. No tests/dependencies/production beyond the assigned contract scope were changed or installed. All owned test roots follow existing lstat/realpath cleanup; no denied path or process termination operation.

## Derived-view checkout line ending correction

- [x] Actual owned CRLF fixture RED. Status: independent fixer review_wave0 added one contract after root assignment. The valid owned matrix first renders with --write-view; only its derived Markdown and bridge are converted to CRLF. Original SRS bytes and all files remain unchanged during default read-only validation. 037bb5/97326/7fdf76 naturally exited1: one FAIL/17filtered9.087seconds, expected readonly0 received1. This is an actual derived-file comparison failure, not a new production or semantic RED.
- [x] Minimal comparison correction. Status: only the derived view/bridge read-only comparison normalizes CRLF to LF. Immutable SRS hashes and execution receipt hashes still digest original bytes. Canonical366 rows, source files and semantic status are unchanged; no renderer write or dependency change.
- [x] Full structural contract GREEN. Status: 8fa734/25716/e167b4 naturally exited0,18PASS32.882seconds. Earlier17PASS22.357 and six-fixture RED remain separate historical results. No E2E/full npm/coverage or benchmark run.
- [x] Independent review/root commit. Status: subsequently completed for candidate mapping/publication; historical checkpoint: source script one comparison line, dedicated contract and this ledger frozen for other reviewers. This correction does not establish semantic acceptance or finish #58.

Exact RED command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/tools/srs-trace-validator.test.ts --testNamePattern 'CRLF derived views'`. GREEN uses the same command without testNamePattern. The owned fixture retains raw pre-read buffers and asserts exact byte equality after readonly before checking exit0; source buffers are separately compared to their pre-conversion originals. No source/receipt SHA normalization and no original or denied path cleanup.


## Final structural candidate review checkpoint

- [x] Independent structural and portability reviews. Status: initial complete candidate was reviewed independently, separate M2 and CRLF fixers supplied actual RED/GREEN, and their final deltas passed fix_lint_diagnostics and review_cert_conflict/review_wave0 cross-reviews. Final18PASS32.882seconds remains separate from earlier17PASS/11PASS and all RED receipts.
- [x] Root structural commit and semantic mapping. Status: subsequently completed for candidate mapping/publication; historical checkpoint: source366 rows remain unverified, bridge entries remain empty and the first25 draft is separate. This review establishes schema/tool correctness only, not issue58 completion or runtime evidence.

Approved title: docs: 요구사항 추적 구조와 검증 도구 추가. This entry records reviews only; it changes no source requirement, trace status or execution receipt.

## First 200 candidate mappings published

- [x] Normalize and independently review batches 05–08. Status: the reviewed drafts for original ordinals 101–200 were applied without deleting clauses, candidates, context, gaps or dispositions. The remaining 166 rows stayed deep-equal to the prior canonical matrix. Structural corrections for two historical out-of-scope rows and one lint source anchor passed a separate review.
- [x] Validate in an owned render root before publication. Status: render and read-only validation both exited 0 in `srs58-render05-08-5jhfqt6s`; 366 rows, 118 candidate links, 166 unchanged rows, zero verified rows and zero runtime evidence. Earlier CLI, schema and pre-copy guard failures remain separate evidence.
- [x] Publish canonical matrix, generated view and bridge. Status: root replaced only the canonical matrix with reviewed staging data, generated both derived files through the single renderer, then passed read-only validation. Final hashes: matrix `6be85f71b2f8c48917b3cfa6b2f2388ad4e341082e28824a66d52440879a7f8b`, Markdown `07a1f94639d401d953db636e77c6fc25475eafdf11dc7404389266b6d41ec59b`, bridge `fb7ecf2a4edbdbc013494ecdef9fb20f69ea4898445c1d744c921fd7cb8e4f3f`. Pre-publication files are preserved under `srs58-first200-publish-749eaa5e973f47afa23238a8baf553cc`.
- [x] Complete remaining166 candidate mapping. Status: later batches were independently reviewed and published; authentic runtime evidence remains a separate open limitation. All current rows remain unverified/candidate and no issue-completion claim is made by this publication.

Proposed publication title: `docs: 원본 요구사항 200개의 테스트 추적 연결`.

## First 300 candidate mappings published

- [x] Normalize and independently review batches 09–12. Status: original ordinals 201–300 were applied after semantic and normalization review; the published first 200 rows and remaining 66 bootstrap rows stayed deep-equal. The stage contains 366 rows, 135 candidate links, zero verified rows and zero runtime evidence.
- [x] Validate the stage without mutating repository outputs. Status: the initial read-only run reported only the expected derived-output mismatch. Owned render and read-only validation both exited 0 under `srs58-render09-12-OBikf0`; protected repository hashes remained unchanged.
- [x] Publish the canonical matrix and derived files. Status: root applied only the reviewed stage, generated the Markdown and test bridge with the single renderer, then passed read-only validation. Final hashes: matrix `845de58d579102389a176cbabfb532551def9b048fe73560482b4827d25d4205`, Markdown `49ee7702ed5154c910ad96996e504fbde16b2736f819a55172e1a69fa905124a`, bridge `9bd2c455965d4b6af275e9c6b438d2fb4383331779ca847b2b3a8d0dd38b24d5`. Previous outputs are preserved under `srs58-first300-publish-f3088c6080b74d2faccca2c3dc5db098`.
- [ ] Publish the final 66 reviewed rows and attach authentic execution evidence. Status: batches 13–15 remain in staging. All current rows remain unverified/candidate; publication is not issue completion.

Proposed publication title: `docs: 원본 요구사항 300개의 테스트 추적 연결`.

## All 366 candidate mappings published

- [x] Complete independent semantic review and normalization for batches 13–15. Status: the final 66 rows retain every original record, clause, limited candidate, context, gap and pending disposition. The first 300 rows stayed deep-equal to their reviewed stage.
- [x] Validate the complete staging data in an owned render root. Status: the initial read-only run reported only the expected derived-output mismatch; owned render and read-only validation exited 0 under `srs58-final366-3y5pLd`. The stage contains 366 rows and 142 candidate links with zero verified links and zero runtime evidence.
- [x] Publish all 366 candidate mappings. Status: root applied the reviewed stage `3ec42db8633a9724f1c5c6cca84365c2c10ec5e54e21f889d1a43313da95a6b1`, generated the Markdown and test bridge, and passed read-only validation. After marking embedded staging metadata as historical, current hashes are matrix `e292d59743b119d0fa1e879db6baae4a3e4be79a3086da066361c9977c3e22c2`, Markdown `5511ee10e51eb4ee39fb349a9c9e5b550c2404bded823d2732988417b97aa4df`, bridge `f8d95ce1b344793fce4f907948a723c4d2e0fc5d9e1a749b6a9d138e24294d18`. Previous outputs are preserved under `srs58-final366-publish-eb1a1802bf204e3f8d668751b0f362b0`.
- [ ] Attach authentic execution evidence and resolve every explicit gap/disposition. Status: all rows remain unverified/candidate. The complete candidate inventory satisfies the mapping draft milestone but does not prove implementation or runtime verification and did not by itself close issue #58 at that checkpoint; root later closed the trace-inventory issue with the explicit limitation recorded below.

Proposed publication title: `docs: 원본 요구사항 366개의 테스트 추적 초안 완성`.

## Semantic draft batch02: original positions 26 through 50

- [x] Read-only bounded mapping draft. Status: `epic61-issue-58-draft-batch-02.json` preserves exactly the next25 sidecar records, their full original objects, immutable source hashes and exact Markdown headings. Current source symbols and inspected test assertion excerpts are recorded; sentence/schema/field/status clauses remain unverified, including contextual behavior.
- [x] Candidate limitations recorded. Status: partial default/metadata roundtrip/static-path assertions are expressly partial. API setup200, native certificate context and authenticated version GET are not promoted to the broader API requirements. Unlinked clauses remain explicit gaps. No unqualified legacy aliases are minted; stable document/version/byte provenance is required before migrating CRLF-sensitive legacy references.
- [x] Independent semantic review and canonical migration. Status: subsequently completed for candidate mapping/publication; historical checkpoint: this is a separate noncanonical draft only; all366 canonical records remain unverified, bridge remains empty, and batch01 stays separate. Other341 records are outside this batch, not dropped. No runtime evidence or verified promotion is claimed.

No tests, validator, build, application or benchmark ran during this mapping window while root #49 Job2 was live. Only source/test text reads and JSON draft generation occurred. Initial stdin JSON-generator receipt98ca56 failed parsing before writing the draft; corrected data-only generator receipt a3e7bc exited0. This scripting mistake is not a behavioral RED or test result. Independent review remains required; no source, existing test, immutable SRS or canonical artifact was modified.


## First100 schema-normalized staging and legacy reference snapshot

- [x] Prepare separate staging. Status: epic61-issue-58-staged-01-04.json retains all366 rows in original order; only first100 receive reviewed draft01..04 clause/candidate/gap data. Other266 remain canonical bootstrap data. All runtime states remain unverified/candidate with empty evidence. Missing schema rationale/modes fields are normalized from the preserved source span/scope; no link is removed to satisfy structure. Parameterized candidates retain exact titles/parameters with titleResolution unresolved pending structural handling.
- [x] Preserve legacy reference bytes. Status: references/plan-remediation-r1.md is an exact copy of existing working bytes SHA cdb672bba16fa8e4b7aed57b9e38b7335638c4f93e3e2b574fa47b2c762ee5e8, protected by one exact-path -text attribute. Adjacent provenance JSON records original path, HEAD, Git object and LF blobSHA120191df133e823bf898329f6670928329b93fed7f26ad2cc47e7d5ba3a2e01b; decoded content differs only by CRLF/LF. Legacy alias digest remains unchanged and references the snapshot with original linkage. Existing legacy document and immutable SRS bytes are untouched.
- [x] Independent staging review, then canonical merge/render gate. Status: subsequently completed for candidate mapping/publication; historical checkpoint: no canonical/view/bridge overwrite, validator/test/build or runtime execution. Draft review.state records actual thread semantic reviewers only: batch02 review_cert_conflict (root-relayed final after wave0 6f9624), batches01/03/04 fix_lint_diagnostics reviewing separate fixer deltas. These are not execution receipts and do not establish full requirements acceptance; normalization staging has its own unreviewed gate.


## First100 canonical publication checkpoint

- [x] Preserve original validation failures. Status: d38fc9/E srs58-staging-validation-HkxIdf recorded three missing row-level gap clauseRefs. Root5001e1 added only originalId references; 07f64b/E srs58-staging-validation-70Ok60 then passed source/schema/alias validation but still exited1 on two expected absent/stale derived outputs. Neither original command is relabeled PASS.
- [x] Independently reviewed staging and owned render. Status: root reports review_cert_conflict normalization PASS; root three-gap metadata correction was independently read-confirmed. d69d8d/E srs58-owned-render-3gXwHo copied62 regular references and generated/read-validated owned derived files with native0 twice. This establishes structure/render portability only, not runtime acceptance.
- [x] Root-authorized first100 canonical publication. Status: 47bdbb/E C:/Users/beom/AppData/Local/Temp/srs58-first100-publish-UonETR first checked approved stage SHA162e0a0a313cb1d83875a7f2b4be4e4293bed0907dacf9c7fff299dc78730a8f, backed up canonical/view/bridge bytes and hashes, then copied the exact staging bytes to canonical. Existing renderer --write-view and default readonly both naturally exited0/signalnull/errornull. Exact commands and raw outputs are preserved in E. No semantic link deletion or validator change.
- [x] Independent final data review/root commit. Status: subsequently completed for candidate mapping/publication; historical checkpoint: all366 rows remain; first100 runtime statuses unverified with74 candidate links, remaining266 deep-equal to prior canonical, verified0/nonempty execution evidence0. Protected SRS/legacy source/snapshot/referenced test hashes unchanged. Staging normalizationReview is retained as its historical pre-publication marker; this checkpoint records subsequent independent review and root publication approval without rewriting the approved SHA.

Canonical SHA162e0a0a313cb1d83875a7f2b4be4e4293bed0907dacf9c7fff299dc78730a8f; derived Markdown b1d41c2a68ff2016f835fefc16f4c3e02c616578253e25c733aec6d3f9918311; bridge37ac21f0fbaf3f63759b07632a994be53dff9922029244967d9a098e7c7cc886. No test/build/full npm execution, process termination or existing denied artifact access. Source requirements and runtime evidence remain distinct from this first100 mapping publication.

## Operational closure and limitations

- [x] Commit/push trace inventory. Status: final data commitd4efb8948c567c36b1a5e222580e5e3a9ec85472 remotely pushed;366 rows,142candidate links,108linked/258gap-only rows and97 unique tests. Original source, failed structural executions and gap/disposition records preserved.
- [x] Close and notify. Status: CLOSED2026-09-09T02:32:44Z/comment5594889018, Telegram4002(68/64). Runtimeverified0 and execution evidence0; closure explicitly does not certify all366 implementation/runtime requirements. Authentic evidence and unresolved dispositions remain visible follow-up limitations. #52 coverage remains a separate uncompleted gate.
