# Issue #45 — Client framing boundary and owned reconnect

Status: complete; Joint final integration35a5d5f30c4b2e894ca9daf266c864bbc7871afa pushed/remote verified, sourcefdc4ad6;12suites/85tests PASS164.236seconds, natural exit0; forced build PASS. GitHub CLOSED2026-09-08T07:11:13Z, comment5580848645; root Telegram3975(66/44). Historical stage/review/pending statements below record the execution sequence; current progress SSOT is the main execution plan.
Research: main `epic61-client-control-research.md`, approved additive CtrlReadResult API and joint #45 → #46 milestone. Writable: CtrlPacket streamer/result, TunnelClient, dedicated tests/fixture and ledger only. Neither issue closes until both stages are verified.

- [x] Read issues45/46, approved API and reuse. Status: existing parser/queue, withLegacyIds real server/peer fixture and TTTClient scheduler are reusable; no new framing parser or reconnect loop.
- [x] Observe result/legacy compatibility and actual client cleanup/reconnect/late-owner RED. Status: missing additive API and corrected real consumer failures below, before production changes.
- [x] Minimum shared drain/result and owned client teardown. Status: one parser/drain, additive result API and current-owner single terminal path; metadata unchanged.
- [x] Focused regressions and forced build. Status:11focused tests plus separate28compatibility tests and forced build passed.
- [x] Independent first-stage reviews. Status: review_wave0 and review_cert_conflict final M1 rereviews PASS, zero Critical/High/Medium/Low findings and exact-title PASS; root stage commit/integration follows.
- [x] M1 actual nonzero endpoint wait-queue control. Status: separate fixer observed baseline behavior RED and identical original candidate GREEN; both independent rereviews PASS.

## Separate fixer M1: actual endpoint bytes waiting at malformed teardown

Root assigned `fix_lint_diagnostics` to supplement the two reviewers' missing nonzero-wait-payload evidence. Original author remains `fix_supply15`. Before adding/running the new control, receipt a69432 created owned evidence root `C:/Users/beom/AppData/Local/Temp/control-45-m1-evidence-ZhCp7D`. It preserves byte-exact candidate and baseline copies of only `src/client/TunnelClient.ts` and `src/commons/CtrlPacket.ts`, `production.patch`, and `manifest.json` with SHA256. HEAD was `f3c2a10fc1fffeeb561249a41ea7feea80b2b80e`; patch SHA256 is `70eafb3d9eea3b8315178d8f1e17a5d572bbc4e7b9a2a4cde57fb7fff03172d2`. Only these two task-owned production files were restored to HEAD for RED. No other production source was changed.

The added test is `real queued endpoint payload is cleared once at malformed control teardown and never replayed after scheduled reconnect` in the existing control-framing test. It reuses the actual v2 client/server, live baseline echo and independent legacy sibling. For one new session, a disclosed callback delegate changes only the endpoint port to a newly owned loopback TCP listener, while calling the original EndpointPool connection implementation. That real listener sends `owned-endpoint-waiting-payload-45`. An explicitly approved barrier holds the actual SuccessOfOpenSession control packet and its callback before OnlineSession/ACK; it never synthesizes success, assigns counters, or manually populates queues. Actual endpoint receive flows through the original client callbacks and sendData into the waiting queue. The held packet is cancelled with its old owner and never replayed on the replacement.

New control RED command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/component/client/control-framing.test.ts --testNamePattern 'real queued endpoint payload'`. Receipt77d455 exited1 naturally,1failed/5filtered,3.807s. Owned facts root `C:/Users/beom/AppData/Local/Temp/control45-queued-4YdJ3o/facts.json` records before/cleanup/after facts before the final verdict: actual marker33bytes, queueBytes33, handlers2, queues1, one real endpoint connection, held command7, no forward bytes. Baseline cleared maps/bytes and the retained queue and performed scheduler/fresh/sibling echoes, but generic procError was1, callback error was absent, and control socket was not yet destroyed at that callback. The test failed the generic-path assertion. This is real boundary behavior RED under a populated queue, not a claim that baseline could not eventually drain that queue, and not a missing API or setup failure. Earlier missing-API and fixture-correction history below remains unchanged.

Receipt22a8f6 verified both current files still matched their baseline SHA256, then restored the original candidate byte copies. Candidate hashes remain `0f19d290b1e3072ae0c9d4085642d2900dbd44c79c5cd31ea284fc55bf6ec9f4` (TunnelClient) and `8ec3068bd172726ebde59cdda5280f0d34474bb4fdc0ee7a17f5857db583f8cf` (CtrlPacket). No new production implementation was necessary or written.

The identical new control command then passed (ece306), natural exit0,1passed/5filtered,3.536s. `C:/Users/beom/AppData/Local/Temp/control45-queued-Du50e2/facts.json` preserves actual33byte precondition, one terminal callback with Error, zero generic procError, zero handler/queue maps and total bytes, retained wait-object bytes0/queue0, and destroyed control/data sockets at the terminal callback. Existing scheduling made a new authenticated TunnelClient; actual fresh forward echo and healthy sibling echo completed. Read-only delegating observation of the old data handler saw no old marker send, the pending forward socket received nothing, and the fresh socket received no late extra payload during the sibling roundtrip. The owned endpoint was closed. All evidence roots are preserved; the fixture closes only its actual owned sockets/listener.

Final focused command after supplementation: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/commons/ctrl-read-result.test.ts test/component/client/control-framing.test.ts`. Receiptsf474ab/cf5e77, session18102 polled through natural exit0: two suites/12tests PASS,18.824s. The new full-run facts remain at `C:/Users/beom/AppData/Local/Temp/control45-queued-cb7Dl6/facts.json`. The original five-file write set remains; this supplement changes only the dedicated control test and ledger beyond identical production candidate restoration. No stage/commit/push/issue post, denied path access, benchmark, metadata/#46 change or new reconnect implementation occurred. Source and ledger are frozen for the root's two independent reviewers; prior28compatibility/build receipts below retain their original scope/time.

## Original author implementation and historical receipts

Result signature: readCtrlPacketResult(Buffer) returns packets plus optional {kind:framing|overflow,cause:Error}. Fatal input returns no packets, clears its pending batch and does not invoke optional overflow callback. Legacy default throws/callback once and parser state are preserved. Incomplete/Discarded semantics remain; no resynchronization, producer changes or metadata changes.

Actual consumer plan: existing owned real server/raw legacy sibling plus actual v2 TTTClient; maintain live endpoint echo, inject malformed framing from its real server control socket, observe only that client termination once, cleared queues and scheduler reconnect/fresh echo. Delegating receive/procError/state observations distinguish protocol handling from generic socket exceptions; late old terminal is explicitly injected and must not clear the new owner. This is narrow control ownership, not completion of general #25 data races. #46 metadata remains unchanged and separately gated.

RED command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/commons/ctrl-read-result.test.ts test/component/client/control-framing.test.ts`.
36956 exited1 naturally,10failed/1passed30.631seconds. Five result tests report missing API, legacy queued-buffer throw control passes. Initial actual-client tests incorrectly waited for a new handler on the old TunnelClient instance; TTTClient's real scheduler instead constructs a new instance. Those five initial reconnect-deadline failures are fixture diagnosis, not accepted defect RED.
Corrected consumer-only command (same explicit path)84721 exited1 naturally,5failed15.958seconds before production edits. Four real malformed-frame cases completed scheduled reconnect/fresh echo and sibling echo but delegated SocketHandler.procError was called1 rather than0; late terminal on genuine old handler added a third close callback rather than preserving2. No manual reconnect or fabricated replacement used. Overflow uses an explicitly small existing streamer maxPendingBytes on the owned handler, not a large memory-pressure experiment. No metadata or process-crash claim.

GREEN40096: initial two-path command after minimum implementation exited0 naturally, two suites/11tests17.526seconds. No callback on result overflow, legacy error wrappers retained; fatal batches reset/drop collected packets; incomplete and discarded-short-command controls pass. Actual malformed prefixes/command/length/overflow now avoid generic procError, notify once with an Error and reconnect through existing TTTClient scheduler to a new TunnelClient. Live data echo before/after and unaffected sibling echo pass; delayed terminal on the genuine old handler does not notify again or tear down the new client.

Compatibility command63394:
`node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/commons/ctrl-read-result.test.ts test/commons/r2-req-01-ctrl-payload-limit.test.ts test/commons/r2-req-03-streamer-overflow.test.ts test/unit/commons/CtrlPacket.test.ts test/component/server/ProtocolV2.test.ts test/unit/client/req-14-connect-race.test.ts`.
Natural exit0, six suites/28tests22.749seconds. This separate run preserves structural payload/max/legacy overflow, protocol negotiation and existing actual data-connect race controls. `npm run build -- --force`6f6991 exited0. No whole-suite or #46 metadata success claim.

Evidence limit for review: live data handlers exist from the retained actual echo socket, and the terminal observer asserts owned maps/queue bytes are zero. This fixture does not deliberately seed nonzero waiting-queue bytes, so it is not a measured nonzero-queue-drain test. It does exercise real scheduling and native error callbacks; the old-terminal replay is a disclosed direct callback using the real old handler. No manual reconnect or clock mocking was used. Root may require a bounded independently authored coverage supplement before approval.

Five-file scope: two production files, two dedicated tests and this ledger. Source frozen; proposed title `fix: 클라이언트 제어 프레임 오류를 소유 연결에서 정리`. Do not stage/commit/start #46 until root releases that stage after the independent review gate. #45/#46 issue closures and notifications remain jointly gated.

## Final independent rereview checkpoint

Root confirmed review_wave0 and review_cert_conflict final code/content/exact-title rereviews PASS, zero Critical/High/Medium/Low findings. Neither reviewer executed additional tests in this final rereview. review_wave0 directly compared both production SHA256 values with the preserved candidate and read actual populated-queue RED/GREEN facts. Baseline already cleared queue bytes; its failure was generic error handling, missing classified error and control socket state at notification. The GREEN adds no false claim of newly discovered baseline queue leakage.
Root may commit/integrate the #45 stage, but neither #45 nor #46 may close or receive its completion Telegram until both stages are verified. This reviewer-authored update changes the ledger only; production and tests remain frozen.

## Root stage integration

- [x] Review/integrate/push #45 stage. Status: source 89e66e2 -> main 07c083d3dc02a382511c95024ed2d32b148c55a2 remote verified; root nine suites/52 PASS, 41.037 seconds, natural exit 0; build PASS.
- [x] Joint #45/#46 verification and individual closure/notification. Status: complete; Joint final integration35a5d5f30c4b2e894ca9daf266c864bbc7871afa pushed/remote verified, sourcefdc4ad6;12suites/85tests PASS164.236seconds, natural exit0; forced build PASS. GitHub CLOSED2026-09-08T07:11:13Z, comment5580848645; root Telegram3975(66/44).

## Joint operational completion

- [x] Final joint verification, integration/push and individual closure/notification. Status: Joint final integration35a5d5f30c4b2e894ca9daf266c864bbc7871afa pushed/remote verified, sourcefdc4ad6;12suites/85tests PASS164.236seconds, natural exit0; forced build PASS. GitHub CLOSED2026-09-08T07:11:13Z, comment5580848645; root Telegram3975(66/44).
Root supplied these operational receipts. Earlier stage-only integration, original RED, independent test-only correction and pending gate records are historical; neither issue remains OPEN or awaiting notification. The final count at joint completion was45CLOSED/45notified of66; current active work is recorded only in the execution plan. #28/#40/#29 boundaries remain unchanged.
