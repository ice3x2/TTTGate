# Issue #59 — Lint subprocess contracts

Status: remediation complete and both independent final rereviews PASS; source frozen for root commit/integration. Original execution failures and separate dependency correction remain preserved.
Owner fix_supply15; worktree `C:/Work/git/_Snoworca/TTTGate-epic61-process`, branch `fix/epic61-lint-process-contract`, base `f2400463f7c2cb0fd31e4030418101c7fd813cfd`.
Approved research: main `epic61-issue-59-research.md`. Scope: two tools lint tests, small shared test helper, dedicated process tests and this ledger. Scanner/package/production timeout and #60 preserved; no old temporary directory deletion.

- [x] Read current callers and reuse opportunities. Status: existing synchronous child pattern reused; broader smoke-process lifecycle unnecessary.
- [x] Observe parent-strict warn RED and real process/report contract RED. Status: receipts below precede helper/caller edits.
- [x] Minimum helper/caller repair and focused GREEN. Status: after separately reviewed #43 lint dependency, final12PASS6.748seconds natural exit0.
- [x] Independent reviews. Status: review_wave0 and review_cert_conflict final code/content/exact-title rereviews PASS, zero Critical/High/Medium/Low findings; root commit/integration pending.
- [x] Address review M1 malformed JSON diagnostics and LOW1 silent owned cleanup. Status: implemented after RED; focused GREEN below, independent re-review pending.

Historical abnormal exit under load remains unclassified. New deterministic failure fixtures do not prove its cause or resolution. All new fixture files and reports use newly owned system temporary roots. Existing main/release `test/.tmp-req04-lint` is untouched and will not be executed or cleaned through another path.

RED command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/tools/lint-process-contract.test.ts`.
Initial receipt72760e exited1, six failures3.399s: actual inherited-strict warn failed status1; remaining cases were missing-helper contract failures, not real-process failure evidence. Before implementation, corrected tests temporarily used the existing raw spawnSync caller pattern when the helper was absent. Receipt6d699f exited1 naturally, six failures3.905s: warn remained status1; actual nonzero/stderr, nonexistent executable, bounded timeout, empty summary and strict-without-violation cases were not rejected. This test-only baseline adapter was removed after implementation; no mock process results were used.

Implementation: shared helper preserves process environment except explicit test-owned strict mode, uses owned cwd/report directory and existing30s default. It requires expected status, no signal/error, a completed summary consistent with emitted violations and persisted report. Failures include executable/args/cwd/elapsed/status/signal/error/stderr/stdout. Two existing tools callers reuse it; strict violation requires exact1. No script/package/timeout policy changes.

Focused command adds existing `lint-auth-compare.test.ts` and `req-21-lint-error.test.ts` to the RED command. Receipt1a1067 exited1 naturally: 11 PASS/one FAIL5.934s. Actual project strict scan returned status1 with no signal/error, not a subprocess infrastructure failure. To expose its actual violation, stdout was added to diagnostic context and only the failing project case rerun (`--testNamePattern '실제 프로젝트'`): receipt3cfdc5 exited1 naturally, one FAIL/two filtered,1.102s. Scanner reports65files/two violations in existing `src/commons/DataStatePacket.ts` format comparisons with literal `token` (including line72). Root is notified for independent classification; no rule suppression, expectation weakening or production edits are made in #59.

The actual process/report fixtures passed in the focused command. No all-green claim is made while the project scan failure remains. Neither these fixtures nor environment isolation establishes the historical abnormal exit's OS/load cause. Next step: root classification/dependency, then final focused run and two independent reviews. Proposed title: `test: 인증 비교 린트의 프로세스 실패와 검사 결과 구분`.

Final dependency/results: root independently classified the two public discriminator comparisons and integrated the two-comment #43 supplement. Authorized source `369952ce2ba232ea3297fb3fd9d18fd7022291e7` was cherry-picked as local `8d12b47` without changing #59's dirty files. The full focused three-file command above then passed (767eab): three suites/12tests,6.748seconds,natural exit0, including the formerly failing actual project strict scan. This new command does not retrospectively change1a1067 or3cfdc5 failures. No scanner change, test skip, retry of an unexplained failure or old-temp deletion occurred. Source is frozen for two independent reviews; historical pending paragraphs describe earlier checkpoints.

Review remediation by `fix_lint_diagnostics` (M1 and LOW1 only): added exit-zero owned child cases `preserves execution context for malformed stdout JSON from an exit-zero child` and the corresponding `report` case. The child itself writes its report and stdout plus a stderr marker under its new system-temp root. RED command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/tools/lint-process-contract.test.ts --testNamePattern 'malformed|owned cleanup'`. Receipt638589 exited1 naturally,3failed/6filtered,1.030s: both malformed cases reached actual JSON.parse and exposed only SyntaxError without process context. The third failure was a missing cleanup helper, not evidence of an actual OS deletion failure. JSON parsing now rethrows with the original error as cause, source name and the existing complete command/cwd/elapsed/status/signal/error/code/stderr/stdout context. No fallback or successful violation verdict is introduced.

An initial cleanup implementation made an already absent path fail; root required preservation of prior force:true semantics. That cleanup implementation was removed before restarting its test-first cycle. Superseded focused receipts53a8ba/3da5ad and abc6dd/402625 passed15tests in6.896s and7.210s respectively; these are not the final cleanup contract. Replacement cleanup RED command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/tools/lint-process-contract.test.ts --testNamePattern 'owned cleanup'`. Receipt2137f2 exited1 naturally,2failed/8filtered,0.661s, both due to absent helper. These are missing-helper contract REDs, not real OS deletion failure evidence.

Final LOW1 contracts: `owned cleanup reports its exact path when ownership validation fails` creates an actual junction strictly inside the owned canonical temp root pointing to that same root, expects the alias/canonical mismatch to fail before deletion with the exact path, and checks that the alias remains. `owned cleanup permits an already absent owned path` preserves idempotent cleanup. All three suites now reuse one cleanup helper; newly allocated roots are canonicalized on creation, cleanup compares normalized and real paths before recursive removal, and exceptions are rethrown with path and original cause. There is no mock, injected OS failure, historical-temp cleanup or claim that a real OS rm failure was reproduced. The catch covers actual filesystem cleanup errors as well as ownership failures.

Final remediation GREEN command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/tools/lint-process-contract.test.ts test/unit/tools/lint-auth-compare.test.ts test/unit/tools/req-21-lint-error.test.ts`. Receipts027b6b/76de13, session58303 polled through natural exit0: three suites/16tests PASS,8.251s. Only the authorized four test/helper files and this ledger changed in remediation; production, scanner, package and timeout policy did not change. Historical load-related abnormal exit remains unclassified. Remediation is frozen for the root's two independent reviewers; no stage/commit/push/issue post was performed.

## Final independent rereview receipts

Root confirmed review_wave0 and review_cert_conflict final rereviews both PASS, zero Critical/High/Medium/Low findings. M1 is resolved by preserving malformed stdout/report JSON cause and execution context; LOW is resolved by visible owned-root cleanup errors, canonical creation/validation and idempotent absent-root handling. Existing mode, semantic summary/report checks, scanner rules and historical-cause limitations remain unchanged.
Neither final reviewer performed an additional execution; both assessed the actual diff, declared RED distinctions and existing final three-suite/16-test PASS receipt. The original exact proposed title recorded above remains PASS. Root commit/integration/push/closure remain pending. This entry was written by review_wave0; source/helper/test files were not edited.
