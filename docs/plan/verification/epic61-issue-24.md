# Issue #24 — Data terminal ownership

Status: complete; source643afe2 -> mainc497ecd5feb6403315894b7c7d928b6a8aca3776 pushed/remote verified; GitHub CLOSED2026-09-08T09:36:26Z, comment5582813455; root Telegram3980(66/48). Eight suites/55tests PASS164.834seconds, natural exit0; forced build PASS. Earlier stage/review-pending statements are historical; original failed receipts remain unchanged.
Approved main lifecycle research and root assignment: TunnelClient only plus dedicated tests/fixtures/ledger. #25/#23/#18 remain later gates; no endpoint/server/socket/wire changes.

- [x] Read original issue and latest pending-attempt/terminal API. Status: reuse existing clearWaitBuffer/deleteDataHandler/packet producers and #45 real greeting barrier.
- [x] Observe queued A/B, online and pre-Connected failure RED plus late/repeated terminal controls. Status: exact failures and baseline passing stale-control distinguished below; main assignment confirmed first.
- [x] Minimum pending-attempt ownership and terminal transition. Status: implementation uses existing cleanup and packet paths.
- [x] Regression/build and assigned review gates. Status: six focused PASS, actual50PASS within retained ENOENT-failed command, separate intended3PASS and buildPASS; final full/delta review scopes below.

## Separate fixer M1/M2/M3

- [x] Backup before baseline restoration. Status:27a2ad created owned `C:/Users/beom/AppData/Local/Temp/data24-fixer-EQfFAv` with candidate.ts/baseline.ts/production.patch/manifest.json. Only TunnelClient restored to HEAD35a5d5f. SHA256 candidate=`74cb5033f693e4cc8355525462e2762950a384e8ec3c710c7aa9b198b6398eb0`, baseline=`73afb7809cc91bd248d11a8925c3b3ce05003535e1b84421c58592917c184ab6`.
- [x] Strengthened baseline RED. Status:6df028/7533e7 session55615 natural exit1,4failed/1passed23.293s. Pending A/B were actual owned TCP sockets whose native Connected application callbacks were held; A was not retired by replacement and global stop did not close either pending socket. Actual server-issued refused attempt had pendingBefore=true/ECONNREFUSED/Connected0/applicationbytes0 but no server-consumed failure response. Online localclose remained0. Passing old-handler replay remains a baseline compatibility control.
- [x] Correct M2 fixture before accepted RED. Status: initial run above and cb6852 (1failed/4filtered7.259s) had held0: neither data socket termination nor closing the owned forward socket generated the expected server CloseSession. These are setup failures, not local-cleanup RED. Root approved explicit invocation of the actual server pool's existing sendCloseSession(a,0) producer, holding its real bytes/callback on the actual control socket. Corrected baseline1ffe43 natural exit1,1failed/4filtered4.020s: positive matching held CloseSession, remoteConsumed0, but A active entry remained after native terminal. No claim of spontaneous server notification.
- [x] Identical candidate reapplication and new defect RED. Status:55c386 verified baseline and restored exact candidate hash;55c386/fe701a session30007 natural exit1,2failed/3passed17.313s. M1 A stayed open even after stop; B closed and late A/B callbacks did not re-register (existing global guard control). M2 local cleanup preserved B33bytes, but actual held CloseSession release/consumer completion produced local callback2. M3 already passed: actual server-issued SID/handler, one server-consumed FailOfOpenSession, server pending removal and sibling echo.
- [x] Pending-only remote-close RED before further production edits. Status:8b5a5c natural exit1,1failed/5filtered4.673s. Actual server-issued pending attempt and consumed server CloseSession yielded local1 but pending=true/socketdestroyed=false; releasing held native Connected republished state. This prevents ignoring all missing-active CloseSession packets.
- [x] Minimum TunnelClient correction. Status: retire superseded pending owner before replacement; missing-active CloseSession cleans/notifies only a still-owned pending attempt, while already-completed/unowned sessions receive no duplicate notification. Existing online/drain/explicit endpoint-close paths and producers unchanged. No tombstones/shared framework or other production edits.
- [x] Focused GREEN. Status:bd9731/4a8808 session62082 natural exit0,6testsPASS22.520s. Command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/component/client/data-terminal.test.ts`.
- [x] Final regressions/build. Status: actual tests passed; command-path failure preserved separately below. No stage/commit/push/issue post.
- [x] Final review gates. Status: review_wave0 full review PASS; fix_supply15 independently reviewed only fix_lint_diagnostics correction delta PASS. Both exact-title PASS; root commit/integration pending.

Final regression command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/client/req-14-connect-race.test.ts test/component/client/control-framing.test.ts test/component/client/control-metadata.test.ts test/component/server/ProtocolV2.test.ts test/component/server/data-handshake-payload.test.ts test/component/socket-owner-terminal.test.ts test/component/control-payload.test.ts`. The last path was a fixer invocation error (file does not exist). Receipts362b18/f133f6, session9125 polled through natural exit1: one ENOENT suite failed, the six actual suites/50tests PASS,170.221s. This command is not GREEN and was not rerun as a whole. The missing intended suite was then executed separately with exact command `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/component/control-payload-consumers.test.ts`:c16ce5 natural exit0,one suite/3testsPASS5.081s. This preserves actual small-byte/control/drain coverage without relabeling the earlier error or inventing one combined passing run. #45 populated-queue regression evidence: `C:/Users/beom/AppData/Local/Temp/control45-queued-hMVSW6`.

Forced build: `npm run build -- --force`,f8475e natural exit0 (existing npm always-auth warnings). Original author4PASS14.053s and compatibility5suites43PASS147.841s remain historical below. Final write scope is unchanged four files; this fixer changed TunnelClient, data-terminal.test.ts and this ledger only, with the original dedicated fixture reused. No live exec handle remains.

Separate fixer=`fix_lint_diagnostics`; original author=`fix_supply15`. Facts are preserved in new system-temp roots (each facts.json): baseline pending `data24-pending-eXoNqq`, refused `data24-refused-vNcIHQ`; held0 setups `data24-queued-ym4Gcy`/`data24-queued-S4ao7P`; corrected baseline queued `data24-queued-oFmFn7`; original candidate pending `data24-pending-zjadSW`, queued `data24-queued-LuSdET`, refused `data24-refused-jUgK9d`; candidate pending-close RED `data24-pending-close-uPs0j7`; final focused pending `data24-pending-TKEA5i`, pending-close `data24-pending-close-P1agjd`, queued `data24-queued-hsQ6gA`, refused `data24-refused-cYeqfQ`. All evidence roots preserved.

The pending tests hold delivery of real native Connected callbacks, without fabricated socket success or protocol writes while held. Global stop closes both sockets before late callback release; active/queue/pending/bytes/peer notifications stay0. M2 requires positive held command/session evidence and actual client-consumer completion after release; A callback remains1, B actual greeting/echo and same-control active sibling survive. M3 routes only one actual server-issued data attempt to a just-released owned port, aborting any successful connection before application Connected. Valid evidence requires ECONNREFUSED/Connected0/applicationbytes0 and actual server consumption/pending removal. TTL60000ms exceeds per-test30000ms cap and bounded3000ms conditions; no TTL-event counter or timer-policy change is claimed. #25/#28/#29/#40, denied paths and benchmarks remain untouched.

## Original author history (retained)

Contract: install attempt before connect, bind actual handler before callback branches, capture state/IDs then settle once. Pre-online sends existing FailOfOpenSession, online existing CloseSession, already Terminated no duplicate. Local handler/queue/endpoint cleanup cannot wait for send callback/ACK. Stale attempt or stale active identity cannot remove a replacement. Global cleanup invalidates pending attempts. Existing explicit endpoint close and small-byte/drain remain unchanged.

Tests will hold target server CloseSession plus real successful endpoint-open callbacks independently, with actual33-byte greeting A and nonzero B on same TunnelClient/control. Observe native terminal before local assertions, aggregate retains B only, B normal ACK/echo and active sibling echo; no TTL/reconnect masking. PreConnected fault routes only one real data connect to owned TLS-reject peer. No synthetic counters/queues or fake socket return. Scope is not general #25 generation ownership.

Initial selected command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/component/client/data-terminal.test.ts`;50530 exit1,3failed10.948seconds. Online localclose0 vs1 is actual RED. Queued marker was accidentally34bytes (not defect RED); corrected original33-byte marker and reran only `--testNamePattern 'queued target'`:37c758 exit1,1failed/2filtered4.899seconds. Both actual A/B queues held33bytes; after observed native A terminal the active-map entry remained. Source still unchanged. Separate `replayed terminal` genuine-old/real-replacement compatibility control6ddabd passes1/3filtered3.977seconds; baseline has no terminal branch, so do not claim this passing stale-control is a RED.

PreConnected fixture discovery: actual TLS rejection produced Connected1 because existing SocketHandler.initSocket emits Connected on TCP connect before secureConnect. This does not satisfy approved Connected0 fixture. Root notified with source evidence and asked to approve an alternative owned failure route; no SocketHandler/source edit, no falsely accepted RED. Pending decision before implementation. TTL is explicitly60000ms (check1000) via existing API, beyond bounded fault window; control identity is checked and no reconnect manually invoked.

Root and review_cert_conflict approved alternative preConnected fault: bind new owned loopbackport0, record port, await close, immediately connect; it is no longer reserved. Intercept any successful Connected before forwarding to the application, destroy and invalidate fixture. Valid run must show ECONNREFUSED/native terminal/Connected0/applicationbytes0. Actual selected REDbc8a80 exit1,1failed/3filtered5.158seconds: all fault-validity assertions passed, then localclose0 vs1 failed before production changes. No arbitrary retry or foreign endpoint was used. Runtime scope remained TunnelClient only.

Minimum implementation then added pending attempt ownership before connect, first callback identity binding, current active/pending identity guard and settled terminal handling. Existing deleteDataHandler/clearWaitBuffer cleanup is reused with identity checks; global destruction invalidates pending attempts. State captured before cleanup selects existing FailOfOpenSession for pre-online or existing CloseSession path online; local cleanup happens without send completion/ACK. First four-case GREEN attempt71455 is running; not yet a PASS.

71455 final natural exit0: four tests14.053seconds. Actual queued A/B each33bytes; A terminal removes A while aggregate remains B33, B opens through held real success callback/normalACK and receives its greeting exactly once plus echo; existing same-owner active sibling echoes and control/TunnelClient identity remains. Online terminal emits CloseSession once; actual replacement ignores old terminal; explicitly already-Terminated replacement sends no additional packet. Owned refused connection observes zeroConnected/applicationbytes, ECONNREFUSED and one local/peer-send failure notification. Some callbacks/packet observations are explicit test delegates, not independent peer-receipt evidence.

Forced build `npm run build -- --force`dededd exit0. Compatibility47528:
`node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/client/req-14-connect-race.test.ts test/component/client/control-framing.test.ts test/component/client/control-metadata.test.ts test/component/server/ProtocolV2.test.ts test/component/server/data-handshake-payload.test.ts`.
Running, no PASS claim yet. Exact proposal title: `fix: 클라이언트 데이터 연결 종료 시 소유 세션 정리`.

Review limits: remote target CloseSession observer holds any matching native server notification and only releases after local assertions, but does not yet require a positive held-packet count. The preConnected failure test observes real outgoing FailOfOpenSession bytes at client send, not an asserted server receive callback. Raw expiry is excluded by60000msTTL and bounded fault waits rather than an explicit TTL-event counter. These limits are disclosed for independent acceptance/coverage review; do not claim stronger evidence. No production outside TunnelClient changed. New dedicated fixture reuses the #45 attachment body because extracting the already-reviewed test was outside assigned writes; all network/server setup still delegates shared withLegacyIds.

Final47528 natural exit0: five suites/43tests147.841seconds. Existing client data-connect race,45control/actualqueued ownership,46metadata,ProtocolV2 and42initialpayload compatibility passed. This is separate from the four new focused cases, not one combined47-test run. Root coordinates independent review/coverage disposition; no stage/commit/closure and no successor started. All handles are terminal; four scoped files remain frozen.

## Final scoped reviewer receipts

review_wave0 independently reviewed the full original #24/approved contract/final code/tests/evidence: PASS, zero Critical/High/Medium/Low findings and exact-title PASS. Root separately confirmed fix_supply15 reviewed the independent fixer fix_lint_diagnostics delta with PASS and exact-title PASS; this is not original-author approval of its own whole change. Neither reviewer performed additional execution in these final reviews.
The six focused tests, six actual suites/50 passing tests inside the ENOENT-failed command, separately executed intended three-test suite and build remain distinct receipts. No failed command is relabeled all-green. Source/test remain unchanged; root controls subsequent commit/integration/closure. This reviewer-authored entry changes only the ledger.

## Operational completion

- [x] Root integration/push/closure/notification. Status: source643afe2 -> mainc497ecd5feb6403315894b7c7d928b6a8aca3776 pushed/remote verified; GitHub CLOSED2026-09-08T09:36:26Z, comment5582813455; root Telegram3980(66/48). Eight suites/55tests PASS164.834seconds, natural exit0; forced build PASS.
Root supplied these operational facts. At closure the epic reached48CLOSED/48notified of66. Original setup/ENOENT failed command, actual50PASS and separately3PASS remain separate historical evidence; the new final8suites55PASS is a distinct execution. No remaining #24 gate; #25 awaits independent design reconfirmation/assignment. #28/#40/#29 boundaries unchanged.
