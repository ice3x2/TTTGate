# Issue #72 — Preserve invalid existing server configuration

Status: complete; source5ba80f6 -> maina64a901f138dc2831d0006b8a107d8d321458490 pushed/remote verified; eight suites76testsPASS179.243seconds naturalexit0/forcedbuildPASS; CLOSED2026-09-08T11:54:00Z comment5584718146 Telegram3989(67/56). Earlier implementation/review/pending clauses are historical; current state is execution Status/Resume.
Read approved main `epic61-invalid-config-discovery.md` and original72. Scope only ServerOptionStore/ServerApp plus dedicated owned tests/driver/ledger. File/Files/cert/TTTServer/YAML/TTL/buffer policy unchanged.

- [x] Read exact loadStatus/readServerOption/ready-only invariant and mutation guards. Status: reuse existing validators/atomic batch and reset flow; no placeholder configuration.
- [x] Observe actual owned file/child startup and non-ready entry RED. Status: actual child/store/API failures below before implementation.
- [x] Minimum two-file implementation. Status: load status/guards/local publication and pre-cert startup result gate implemented; further review pending.
- [ ] Focused legacy/rollback/startup regressions/build/two reviews. Status: pending; no commit.

Contract: only confirmed ENOENT creates defaults. Existing invalid/nonfile/read-failure refuses normal startup before cert load/CLI persistence/listeners and preserves config/revision bytes. Validate local candidate before publication. Non-ready mutations fail before side effects; optional revision defaults evaluated after guards. Ready-only legacy getters throw only bounded caller invariant error; normal invalid startup uses results, never getter throw as flow. Explicit authorized reset remains recovery and publishes ready only after durable writes. Existing reset authorization/destructive scope retained.

Owned process safety: only newly created roots and actual child processes; all handles observed to natural exit. No global Node termination, denied artifacts or benchmarks. Read-error injection is scoped to the owned config file and disclosed; no user files or permission changes.

RED: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/unit/server/invalid-config-admission.test.ts test/component/invalid-config.test.ts`, f8bd5f terminal1,20failed/2passed6.897seconds before either production file changed. Tests use actual fresh-root Store/real child process, missing additive status/read API and mutation/invariant failures; startup child passed the unwanted cert-load barrier. Initial child-only ab8c9c8failed7.925seconds is separately retained. The late invalid row changed from an unsupported protocol string to a negative forwardPort before the accepted run because existing verifier does not reject protocol enums; no protocol policy was tightened.

First GREEN46d0fe same two paths:22PASS6.901seconds natural0; forced buildfd78fd0. An additional nested httpOption.bodyRewriteRules string test f46581 failed1/8filtered1.123seconds (ownedchild unexpected TypeError/exit1 instead of bounded validation result) before adding the narrow prevalidation structure checks needed by existing .filter consumers. No API verifier or YAML library policy changed. All load normalization runs on a local candidate; no invalid candidate is assigned to _serverOption.

Implementation: only confirmed ENOENT default creation; read/nonfile/empty/YAMLParseError/validation report nonready. Existing YAML native parser boundary catches syntax class only, no raw YAML log. Initialization/reset uses existing atomic batch, publishes option/revision/status together only after writes. All non-reset write entrypoints are guarded; ready-only getters assert invariant, and ServerApp uses readServerOption before cert load/CLI override/listeners.

Broader53761 command:
`node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/component/invalid-config.test.ts test/unit/server/invalid-config-admission.test.ts test/unit/server/ServerOptionStore.test.ts test/unit/server/ServerOptionStore.revision.test.ts test/unit/server/ServerCliOptions.test.ts test/component/server/admin/configuration-rollback.test.ts test/component/server/admin/compound-tls-save.test.ts`.
Running; no PASS claimed. Actual child roots/facts are preserved under printed system-temp config72-* paths. Tests for read error inject only readFileSync of that owned server.yaml and restore it; all other reads remain real. Startup uses a declared cert.load throwing observer to stop baseline before real downstream side effects; this proves boundary nonentry, not actual valid/reset listener startup. Valid/missing/reset Store controls are real file operations; a successful ServerApp listener control and failed reset/bootstrap write injection are not yet in this candidate's evidence. These gaps must be assessed before acceptance. Exact title proposal: `fix: 잘못된 기존 서버 설정을 보존하고 시작 거부`.

Further explicit reset-write-failure control d430c2 selected1PASS14filtered0.437s: injected Files.writeAtomicBatchSync error propagates, store remains nonready/read-result false/save false. This is test-only coverage of the existing candidate, not new production RED; the callback is restored in finally. No successful listener-start control added yet. After nested shape guard, forced build a9563c failed TS18048/TS7053 on key-based property access. Equivalent explicit three-field value array removes the typing error without changing the validation decision; final forced build6babc2 exited0. This final typing-only correction occurred while53761 was running and is disclosed rather than treating the earlier compiler PASS as final-source proof.

53761 completed naturally with exit0: seven suites66tests176.612seconds (compound142.043s, rollback26.56s among them). All handles are terminal. This objective result does not resolve review findings. Root relayed preliminary review_wave0 M1 missing tunnelingOptions compatibility, M2 numeric adminBindHost normal validation escaping as TypeError, and M3 actual valid/reset startup/bootstrap-write failure coverage. These findings await the second independent review and a different RED-first fixer; the original author is explicitly frozen and makes no further source changes. Existing source/test/build failures and intermediate results remain above. No commit/closure or readiness acceptance claimed.

## Independent M1/M2/M3/M4 correction checkpoint

Status: complete; source5ba80f6 -> maina64a901f138dc2831d0006b8a107d8d321458490 pushed/remote verified; eight suites76testsPASS179.243seconds naturalexit0/forcedbuildPASS; CLOSED2026-09-08T11:54:00Z comment5584718146 Telegram3989(67/56). Earlier implementation/review/pending clauses are historical; current state is execution Status/Resume.

- [x] Preserve author candidate before baseline comparison. Status:66449c new owned E C:/Users/beom/AppData/Local/Temp/tttgate-72-fix-7ffe4450cba8422c92efde590cb91f9c contains both original production files. SHA256 Store319C821B8014A9AACBB153FF5AFBD2A93648C9619CE72B0BB8B4145643C9E040 and ServerApp642012E526A337A564EC28D9473C2D3AB7DF9F9951C950F6DB0A4DCC15E37285. Only these task files restored to branchHEADfc6e286; no other checkout/source touched.
- [x] Correct baseline fixture limitations. Status:5f344e initial4FAIL2.669s included unavailable additive APIs and file-symlink privilege/setup failure, not four behavior REDs. Driver now explicitly labels fallback baseline-api-absent and observes real legacy getter values for comparison. New owned dangling directory junction needs no existing path and preserves its metadata; no dependent node_modules link/deletion involved.
- [x] Baseline then identical candidate RED. Status:01f6ff baseline2PASS/2FAIL3.386s: missing-list and incomplete trusted rows are accepted legacy controls; numeric host and dangling entry fail new refusal contract. Reapplied original candidate with same two hashes5c9cf4, then4FAIL3.407s: legacy configs wrongly reject, numeric adminBindHost escapes as TypeError, dangling entry follows missing/default path. Raw child results/facts preserved in each printed owned config72 root.
- [x] Minimum Store corrections. Status: absent tunnelingOptions retains existing empty-list normalization; incomplete trusted rows retain existing short-circuit filtering. Numeric adminBindHost and unsafe surviving trusted-row trim inputs return explicit load validation result. When stat reportsENOENT, lstat must also prove entry absence before defaults; an existing dangling entry/read failure stays nonready. No valid-symlink policy change. First four-case GREEN e90713 4PASS3.379s. Additional incomplete rows with ignored nonstring fields exposed an overstrict guard35f6861FAIL1.124s; guard then follows existing filter short-circuit order and does not reject rows the legacy filter removes.
- [x] M3 actual startup/CLI and bootstrap failure. Status: existing child driver now delegates real cert.load/AdminServer.listen/TTTServer.start, captures actual listening and owned control connect, applies startup CLI keepAlive23456, then closes owned services naturally. Valid and explicit reset roots allocate owned ephemeral ports; reset fixture overrides only default port routing while delegating real default creation. Initial c66ba2 two failures/one pass3.058s were fixture admin-HTTP policy rejections; preserved root facts/stderr show that cause. Explicit owned-loopback allowLegacyAdminHttp flag then6be51b3PASS3.053s. This does not weaken production TLS policy. Bootstrap atomic-write fault is disclosed injected boundary and surfaces original failure with no config/state overwrite; existing reset-write-failure test remains unchanged, no duplicate invented RED.
- [x] Focused/full build. Status:68612/348c20 two suites31PASS13.705s then forced build naturalexit0. No additional compile failure occurred in this correction; original author's typing history remains above.
- [ ] Two independent rereviews. Status:64595/c13b32 original seven-path regression passed73tests182.424seconds naturalexit0; no commit, source/test/ledger frozen.

Every spawned child now preserves status/signal/error/stdout/stderr in child-result.json alongside facts.json under its unique owned root. Early file-symlink setup failed before child creation; it is not an OS dangling-link RED. The successful dangling-junction contract verifies lstat/readlink and original revision bytes without deleting or traversing any external target. Ordinary invalid-file/read/validation paths retain the original byte preservation tests; missing/default, ready mutation guards, explicit reset and old rollback/compound contracts remain.

Corrected focused command: `node node_modules/jest/bin/jest.js --runInBand --silent --runTestsByPath test/component/invalid-config.test.ts test/unit/server/invalid-config-admission.test.ts`.
Broader64595 uses the original seven paths listed for53761 above; its result must be separately recorded, not retroactively replacing the author's66PASS.
Final broader64595/c13b32: seven suites73testsPASS182.424seconds, natural exit0; compound142.768s, rollback26.086s, new child contracts13.027s. Exact command is the same seven-path53761 command above. This later73PASS supplements, not replaces, original author's66PASS176.612/compile/setup history. New raw child evidence roots from config72-gwzMfl through config72-T6SF2r are individually printed in the terminal receipt; each keeps child-result.json and facts.json. No live process handle remains, no process termination was used.

Final correction title remains the original issue title proposal: `fix: 잘못된 기존 서버 설정을 보존하고 시작 거부`. Scope remains two production files/three dedicated test-driver files/ledger72, six files total. The independent fixer's source changes affect Store only; ServerApp restored byte-identical from the original backup and received no further source edit. Two original reviewers must assess correction facts/limits before root commit/integration.

## Final independent review gates

Root confirmed review_wave0 final full code/content/exact-title PASS and fix_lint_diagnostics final M1-M4 correction rereview PASS, zero Critical/High/Medium/Low findings. Neither performed additional execution for these final reviews. Original author fix_supply15 also reviewed the separate review_cert_conflict fixer delta with PASS; this is not self-approval of the entire original implementation.
Original66PASS, correction31PASS/build, later73PASS and earlier compile/setup failures remain separate unchanged receipts. Actual valid/reset startup, local-candidate readiness/mutation guards, optional legacy compatibility, invalid-shape and dangling-entry refusal, file/revision preservation and raw child receipts satisfy the approved scope. Existing exact proposed title remains PASS. Root commit/integration pending; this update changes only the ledger, no source/test.

## Operational completion

- [x] Root integration, remote verification, closure and notification. Status: source5ba80f6 -> maina64a901f138dc2831d0006b8a107d8d321458490 pushed/remote verified; eight suites76testsPASS179.243seconds naturalexit0/forcedbuildPASS; CLOSED2026-09-08T11:54:00Z comment5584718146 Telegram3989(67/56). Root supplied these receipts; no new test run is claimed by this document update. #18 is now assigned review_cert_conflict/process fix/epic61-session-idle-policy ata64a901; overall epic remains incomplete.
